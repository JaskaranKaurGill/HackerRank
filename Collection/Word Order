# Enter your code here. Read input from STDIN. Print output to STDOUT

d = {}
for i in range(int(input())):
    Word = input()
    Count = 0 
    if (Word in d.keys()):
        d[Word] = d[Word]+1
    else:
        d[Word] = 1
print(len(d))
print(*d.values())
